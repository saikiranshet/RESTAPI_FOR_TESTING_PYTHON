import pytest
import os
from dotenv import load_dotenv
from ..utils.request_gen import APICaller

load_dotenv()


@pytest.fixture
def get_api_obj():
    BASE_URL = os.getenv("BASE_URL")
    # api_obj = APICaller(BASE_URL)
    return BASE_URL